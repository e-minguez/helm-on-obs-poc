replicaCount: 1

image:
  repository: ghcr.io/e-minguez/hello-world-go
  # Empty → defaults to the chart's appVersion (the image release).
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources: {}
